# Seance Backend Configuration

# Server port
port: 3000

# Builder API key hashes (SHA-256)
# Generate key: openssl rand -hex 32
# Get hash: echo -n "YOUR_KEY" | shasum -a 256 | cut -d' ' -f1
builder_key_hashes:
  - "adf1e1bee2a545ca24690755a59ea58af30cf9f86692541a6a932a75dc831334"
