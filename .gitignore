# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml
.state/

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# Cloudflare Tunnel (contains secrets)
cloudflared/credentials.json
cloudflared/config.yml
cloudflared/*.pem

# Builder keys (private key only - public key is in config.yml)
.keys/
.env

# Terraform
*.tfstate
*.tfstate.*
*.tfvars
.terraform/
.terraform.lock.hcl

# Kubernetes
.kube/

# Ansible inventory (generated by Terraform)
ansible/inventory.yml

# Landing page build output
landing-page/dist/
landing-page/node_modules/

# Backend dependencies
backend-fastify/node_modules/
.state/zitadel/app-credentials.env
